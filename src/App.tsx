import { Box } from '@mui/material';

import { Router } from './router/router';

const App = () => {
  return (
    <Box
      width="100%"
      maxWidth="1000px"
      margin="0 auto"
    >
      <Router />
    </Box>
  )
}

export default App;
